apiVersion: apps/v1
kind: Deployment
metadata:
  name: PythonDock
spec:
  replicas: 2
  selector:
    matchLabels:
      app: PythonDock
  template:
    metadata:
      labels:
        app: PythonDock
    spec:
      containers:
      - name: PythonDock
        image: pocust.azurecr.io/PythonDock:#{Build.BuildId}#
        ports:
        - containerPort: 5000
        env:
          - name: PODNAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
      imagePullSecrets:
      - name: registrysecret
---
apiVersion: v1
kind: Service
metadata:
  name: PythonDock
spec:
  type: LoadBalancer
  ports:
  - port: 5000
  selector:
    app: PythonDock
 
